
import {ApiServiceImpl} from "../app/layout/dashboard/components/healthinfo/api.service.impl";

export const environment = {
  production: true
};

export const services = {
  apiService: ApiServiceImpl
}
